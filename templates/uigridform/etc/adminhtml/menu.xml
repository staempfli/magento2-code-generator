<?xml version="1.0"?>
<!--
/**
 * menu
 * @copyright Copyright (c) ${generator.time.year} ${comments.company.name}
 * @author    ${comments.user.mail}
 */
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Backend:etc/menu.xsd">
    <menu>
        <add id="${Vendorname}_${Modulename}::${modelname}" title="${Modelname}" module="${Vendorname}_${Modulename}" sortOrder="10" parent="${Vendor_Module::parentmenu}" action="${vendorname}_${modulename}/${modelname}" resource="${Vendorname}_${Modulename}::${modelname}"/>
    </menu>
</config>
